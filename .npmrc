# @capacitor-firebase/* 8.x lists firebase ^12 as an optional peer.
# The web app stays on firebase 10.13.2; the native plugins do not need that
# JS SDK bump. This keeps installs resolvable without a drive-by upgrade.
legacy-peer-deps=true
